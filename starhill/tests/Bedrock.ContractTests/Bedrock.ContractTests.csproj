<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
    <PackageReference Include="coverlet.collector" />
  </ItemGroup>

  <!--
    Contract tests (Testing Strategy design): snapshot schema integration-event (task 17/F32) — chống breaking
    vô ý; task 20 mở rộng thêm snapshot Error.Code registry. Ref các assembly *.Contracts (public DTO/event).
  -->
  <ItemGroup>
    <ProjectReference Include="$(PlatformSrc)\Bedrock.Domain\Bedrock.Domain.csproj" />
    <ProjectReference Include="$(PlatformSrc)\Bedrock.Messaging.Contracts\Bedrock.Messaging.Contracts.csproj" />
    <ProjectReference Include="..\..\src\Modules\Identity\Identity.Contracts\Identity.Contracts.csproj" />
    <ProjectReference Include="..\..\src\Modules\Identity\Identity.Domain\Identity.Domain.csproj" />
    <!-- P1-11: gác luôn error catalog của module Rooms (RoomsErrors ở tầng Application — nghiệp vụ QR). Thêm module
         mới có error catalog riêng → thêm ProjectReference tương ứng ở đây (snapshot phủ mọi mã lỗi client-facing). -->
    <ProjectReference Include="..\..\src\Modules\Rooms\Rooms.Application\Rooms.Application.csproj" />
    <!-- C-GA.2b: error catalog GuestAccess (GuestAccessErrors — qr_invalid/room_inactive/configuration_unavailable;
         C-GA.4: +session_expired/guest_context_missing). -->
    <ProjectReference Include="..\..\src\Modules\GuestAccess\GuestAccess.Application\GuestAccess.Application.csproj" />
    <!-- D-Rules.2b/3: error catalog Rules (RulesErrors — rules_conflict + validation_error/not_found dùng chung).
         Đóng gap QR-AD-018 (registry PHẢI phủ MỌI catalog module — trước đây thiếu Rules → rules_conflict không gác). -->
    <ProjectReference Include="..\..\src\Modules\Rules\Rules.Application\Rules.Application.csproj" />
    <!-- E-Faq.2: error catalog Faq (FaqErrors — faq_category_not_found/faq_item_not_found/faq_conflict/
         faq_invalid_parent/faq_category_not_empty/faq_item_has_children). Registry phủ MỌI catalog module (QR-AD-018). -->
    <ProjectReference Include="..\..\src\Modules\Faq\Faq.Application\Faq.Application.csproj" />
  </ItemGroup>
</Project>
