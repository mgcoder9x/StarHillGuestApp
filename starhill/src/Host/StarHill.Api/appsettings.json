{
  "_note": "CHỈ non-secret + placeholder (F35, task 19). SECRET (Jwt.Keys[].Secret, mật khẩu DB) KHÔNG commit: dev nạp qua User-Secrets (dotnet user-secrets set 'Jwt:Keys:0:Secret' <base64-32B>), prod nạp qua env/Key Vault/SOPS. Boot sẽ FAIL-FAST (validate-on-start) nếu thiếu secret bắt buộc — đúng thiết kế.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "_note": "MỘT PostgreSQL vật lý, SCHEMA-PER-MODULE (design §1.2/§4.6): cả 3 module trỏ CÙNG database 'starhill'; tách biệt bằng schema (identity/resort_config/rooms) đặt ở DbContext.HasDefaultSchema. Placeholder KHÔNG mật khẩu — dev/prod cung cấp Password qua User-Secrets/env, override từng 'ConnectionStrings:<Module>'.",
    "Identity": "Host=localhost;Port=5432;Database=starhill;Username=postgres",
    "ResortConfig": "Host=localhost;Port=5432;Database=starhill;Username=postgres",
    "Rooms": "Host=localhost;Port=5432;Database=starhill;Username=postgres"
  },
  "Jwt": {
    "ActiveKid": "dev",
    "Issuer": "starhill",
    "Audience": "starhill-clients",
    "AccessTokenLifetimeMinutes": 15,
    "Keys": [
      {
        "Kid": "dev",
        "Secret": ""
      }
    ]
  }
}
