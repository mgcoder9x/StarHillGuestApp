<Project Sdk="Microsoft.NET.Sdk">
  <!--
    ResortConfig.Infrastructure (persistence + composition nửa-Infra — matrix §3.3): reference
    ResortConfig.Application + Bedrock.Infrastructure. KHÔNG ref Bedrock.Api/ResortConfig.Api (Api⊥Infra I7).
  -->
  <ItemGroup>
    <ProjectReference Include="..\ResortConfig.Application\ResortConfig.Application.csproj" />
    <ProjectReference Include="$(PlatformSrc)\Bedrock.Infrastructure\Bedrock.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <!-- Design-time: IDesignTimeDbContextFactory cho `dotnet ef migrations` (per-module §4.6). PrivateAssets=all. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" PrivateAssets="all" />
  </ItemGroup>
</Project>
