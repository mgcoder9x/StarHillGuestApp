<Project Sdk="Microsoft.NET.Sdk">
  <!--
    Concierge.Infrastructure (persistence + composition nửa-Infra — matrix §3.3). K-Con.2: ref Concierge.Application
    (kế thừa Domain+Contracts transitive; mirror Housekeeping/Rules/Faq: Infra→Application). KHÔNG ref
    Bedrock.Api/Concierge.Api (Api⊥Infra I7). SignalR notifier impl ở Api/Host (K-Con.4) — Infra chỉ no-op default.
  -->
  <ItemGroup>
    <ProjectReference Include="..\Concierge.Application\Concierge.Application.csproj" />
    <ProjectReference Include="$(PlatformSrc)\Bedrock.Infrastructure\Bedrock.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" PrivateAssets="all" />
  </ItemGroup>
</Project>
