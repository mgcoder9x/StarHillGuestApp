{
  "_note": "CHỈ non-secret + placeholder (F35, task 19). SECRET (Jwt.Keys[].Secret, mật khẩu DB) KHÔNG commit: dev nạp qua User-Secrets (dotnet user-secrets set 'Jwt:Keys:0:Secret' <base64-32B>), prod nạp qua env/Key Vault/SOPS. Boot sẽ FAIL-FAST (validate-on-start) nếu thiếu secret bắt buộc — đúng thiết kế.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "_note": "Placeholder KHÔNG mật khẩu. Dev/prod cung cấp connection string đầy đủ (kèm Password) qua User-Secrets/env — override khoá 'ConnectionStrings:Identity'.",
    "Identity": "Host=localhost;Port=5432;Database=bedrock_reference_identity;Username=postgres"
  },
  "Jwt": {
    "ActiveKid": "dev",
    "Issuer": "bedrock-reference",
    "Audience": "bedrock-reference-clients",
    "AccessTokenLifetimeMinutes": 15,
    "Keys": [
      {
        "Kid": "dev",
        "Secret": ""
      }
    ]
  }
}
