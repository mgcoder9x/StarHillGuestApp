namespace Identity.Contracts;

/// <summary>Định danh ổn định của bounded context Identity dùng cho keyed composition.</summary>
public static class IdentityModule
{
    public const string PersistenceKey = "identity";
}
