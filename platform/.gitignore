# .NET build artifacts (KHÔNG commit — tái sinh từ source).
bin/
obj/
[Bb]uild/
*.user
*.suo
.vs/

# EF migration bundle = artifact deploy (sinh ở CI/local, KHÔNG commit — có thể ~100MB self-contained).
efbundle*
artifacts/
