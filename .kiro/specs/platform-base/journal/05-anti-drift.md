# 05 — Anti-Drift Mechanism (cơ chế chống lệch — cực mạnh, tự động)

> **Vấn đề drift:** theo thời gian, (1) code lệch design/quyết định; (2) journal lệch design; (3) spec tham chiếu ID không tồn tại (F#/CP#/R#/AD# dangling); (4) lệch format. Review bằng tay KHÔNG đủ mạnh — con người quên.
>
> **Nguyên tắc cực mạnh:** *biến mỗi quyết định/invariant kiểm-được thành GUARD TEST chạy mỗi build → lệch là FAIL BUILD.* Enforcement tự động > tài liệu. Đây là lá chắn mạnh nhất vì nó không dựa trí nhớ và chạy mọi lần `dotnet test`.

## KEYSTONE RULE (luật xương sống)

> **KHÔNG có quyết định code-enforceable nào mà thiếu guard test.**
> Mỗi khi thêm/đổi một AD (hoặc CP) mà code có thể kiểm được → PHẢI thêm guard test tương ứng trong cùng lượt. Nếu chưa kiểm được bằng test (vd quyết định tài liệu thuần) → đánh dấu `MANUAL` trong bản đồ guard và nêu lý do.

## 5 lớp phòng thủ

| Lớp | Cơ chế | Bắt loại drift | Tự động? |
|---|---|---|---|
| L1 — Architecture tests | NetArchTest: dependency matrix, no-business-in-core, CP11, Api⊥Infra | code ↔ design (cấu trúc/ranh giới) | ✅ mỗi build |
| L2 — Decision guard tests | Reflection assert từng AD (Result=class, IntegrationEvent placement, Html namespace...) | code ↔ quyết định journal | ✅ mỗi build |
| L3 — Contract/behavior tests | Unit/integration khoá hành vi (claim mapping, PathMasker, Outbox atomic...) | code ↔ hợp đồng | ✅ mỗi build |
| L4 — **Journal/traceability consistency** | `Bedrock.ArchitectureTests/JournalConsistencyTests` parse markdown journal → enforce INV-1..INV-5 (ID duy nhất+liên tục, mọi AD có guard, ref không dangling, schema đủ trường, CP trong 1..15) | journal tự lệch / ID dangling / thêm AD quên guard | ✅ **mỗi build** (AD-030) |
| L4b — Spec format | `getDiagnostics` 4 file spec + journal (heading/EARS/CP/waves) | lệch format Kiro | ⚙️ chạy tay sau mỗi sửa spec (Kiro-only, không chạy được trong test) |
| L5 — Authority hierarchy | `design.md` là nguồn sự thật; lệch → sửa theo design + ghi journal | design ↔ journal ↔ code | 🧠 quy trình (N-002) |

## Vòng lặp bắt buộc sau MỖI thay đổi (không được bỏ)

1. `dotnet test Platform.slnx` → **build 0 warning + TẤT CẢ test xanh** (gồm L1/L2/L3). Đỏ = có drift/hồi quy → dừng, sửa.
2. `getDiagnostics` trên `requirements.md`/`design.md`/`tasks.md` + journal → 0 lỗi (L4).
3. Có quyết định mới/đổi? → append journal (AD/DV/TO/N) với **Provenance thật** + (nếu code-enforceable) thêm guard test (KEYSTONE RULE).
4. Quyết định cũ bị lật? → set `Superseded by <ID>`, KHÔNG xoá lịch sử.

## Bản đồ Guard — Correctness Properties (CP1–CP15)

| CP | Nội dung | Guard test | Trạng thái |
|---|---|---|---|
| CP1 | no-business-in-core (tên/namespace + literal) | `NoBusinessInCoreTests` (tên/namespace, 5 assembly) + `NoBusinessInCoreLiteralTests` (Mono.Cecil quét ldstr + const, 5 assembly) | ✅ ENFORCED đầy đủ (task 4 + task 20; AD-022 resolved bởi AD-046) |
| CP2 | Api ⊥ Infrastructure | `Bedrock.Api.Tests/ApiBoundaryTests` (+ chiều ngược Infrastructure⊥Api: `DependencyRuleTests`) | ✅ ENFORCED (task 5.5/6) |
| CP3 | Adapter isolation | `AdapterIsolationTests` (RabbitMq adapter chỉ ref Bedrock.Application, KHÔNG Api/Infra/module/EF/ASP.NET/Npgsql; + negative control) | ✅ ENFORCED (task 14) |
| CP4 | Module boundary | `ModuleBoundaryTests` (Contracts thuần DTO; Domain/App ⊥ Infra/Api; + negative control engine bắt dep vào internal module) | ✅ ENFORCED (task 16.3; A→B đầy đủ khi có module thứ 2) |
| CP5 | Single composition root | `ModuleBoundaryTests` (module `.Api` ⊥ mọi Infra; module `.Infra` ⊥ mọi Api → không library nào bắc cầu Api+Infra; chỉ Host exe được) + CP2 | ✅ ENFORCED (task 16.3; xem N-044 lý do enforce từ phía module thay vì nạp web-exe) |
| CP6 | Outbox atomicity | `OutboxWriterTests` (SQLite) + `PostgresOutboxInboxTests.State_and_outbox_commit_and_rollback_together` (Testcontainers/Postgres: state+outbox commit/rollback CÙNG transaction) | ✅ ENFORCED (task 7.2/7.4) |
| CP7 | Rotation atomicity | `RefreshTokenStoreTests` (SQLite: consume-if-not-revoked + consume+add rollback) + `RefreshTokenRotationRaceTests` (Testcontainers/Postgres: 2 consume đồng thời → đúng 1 thắng; insert-fail → consume rollback) | ✅ ENFORCED (task 8.2/8.3) |
| CP8 | Inbox idempotency | `OutboxDispatcherTests` (SQLite first-true/dup-false/khác-consumer) + `PostgresOutboxInboxTests.Concurrent_inbox_mark_is_idempotent_at_db` (4 task đồng thời → đúng 1 thắng, PK guard) | ✅ ENFORCED (task 7.3/7.4) |
| CP9 | Fail-fast missing port | `RequiredPortsValidatorTests` (thiếu port → ném GỘP; scope-aware) | ✅ ENFORCED (task 10.2) |
| CP10 | Correlation unity | `Bedrock.Api.Tests/BedrockPipelineTests` (gửi `traceparent` → header == body.traceId == trace hiện hành; OTel providers registered; metric http.server.request.duration phát) | ✅ ENFORCED (task 5.4 → siết task 18/AD-044) |
| CP11 | use case ⊥ Messaging.Dispatch | `UseCaseSeamTests` (Bedrock) + `ModuleBoundaryTests` (Identity use case) | ✅ ENFORCED |
| CP12 | Error code contract stable | `Bedrock.ContractTests/ErrorCodeSnapshotTests` (snapshot registry Error.Code — reflect Domain lõi + module) | ✅ ENFORCED (task 20) |
| CP13 | Log masking mọi nơi | `PathMaskerTests` + `BedrockPipelineTests` (masked log, no raw token) | ✅ ENFORCED (task 5.4; + posture AD-024 cho framework logging) |
| CP14 | Domain event atomic dispatch | `Bedrock.Infrastructure.Tests/DomainEventDispatchTests` (handler-effect commit cùng transaction; handler ném → rollback; max-depth ném; no-handler no-op) | ✅ ENFORCED (task 6.4) |
| CP15 | Outbox exclusive claim | `PostgresOutboxInboxTests.Concurrent_dispatchers_never_double_claim` (2 dispatcher đồng thời, BatchSize nhỏ → mỗi message publish ĐÚNG 1 lần; FOR UPDATE SKIP LOCKED — AD-049) | ✅ ENFORCED (task 7.4) |

## Bản đồ Guard — Decisions (AD code-enforceable)

> **Bất biến INV-2 (auto):** mỗi `AD-###` trong `01-decisions.md` PHẢI xuất hiện (token đầy đủ `AD-0XX`) trong bảng này — `JournalConsistencyTests` fail build nếu thiếu. Nhờ đó KHÔNG thể thêm quyết định mà quên khai trạng thái guard.

| AD | Quyết định | Guard test | Trạng thái |
|---|---|---|---|
| AD-001 | prefix `Bedrock.*` | (ngầm: mọi arch test dùng namespace `Bedrock.*`) | 🟡 IMPLICIT |
| AD-002 | `Result`/`Result<T>` = sealed class | `DecisionGuardTests.AD002_*` | ✅ ENFORCED |
| AD-003 | dead-letter = cột `dead_lettered_at` (không bảng DLQ) | `OutboxWriterTests` (cột `dead_lettered_at` tồn tại) + `OutboxDispatcherTests` (dead-letter sau MaxAttempts + không claim lại) | ✅ ENFORCED (task 7.2/7.3) |
| AD-004 | Outbox/Inbox per-module | `OutboxWriterTests`/`OutboxDispatcherTests` (helper `AddOutboxInbox` map vào DbContext module trên SQLite) | 🟡 PARTIAL (per-module trong module thật → task 16) |
| AD-005 | tách 2 namespace `Messaging` vs `Messaging.Dispatch` | `UseCaseSeamTests` (CP11) | ✅ ENFORCED |
| AD-006 | `IIntegrationEventTypeRegistry` (EventType→CLR type) | `OutboxDispatcherTests.Registry_resolves_known_eventtype_and_null_for_unknown` | ✅ ENFORCED (task 7.3) |
| AD-007 | domain event dispatch TRƯỚC commit (cùng transaction) | `DomainEventDispatchTests` (CP14) | ✅ ENFORCED (task 6.4) |
| AD-008 | `JwtKeyRingOptions` điểm chung ký/verify | ký: `JwtTokenServiceTests` (kid + rotation); verify: `AuthMechanismTests` (TestHost) | ✅ ENFORCED (task 5.3 + 9.2) |
| AD-009 | phân loại default per-port (degrade `NullAppCache` / fail-loud `Throwing*`) | `ExtensionArchitectureTests` (cache miss-through + 8 port fail-loud throw + override Replace + registry) | ✅ ENFORCED (task 13) |
| AD-010 | refresh-token: cơ chế lõi, bảng schema module | `RefreshTokenStoreTests` (store atomic + helper, SQLite) + `RefreshAccessTokenUseCaseTests` (rotation use case, task 16) + `RefreshTokenRotationRaceTests` (race đa-connection, Postgres) | ✅ ENFORCED (task 8.2/8.3/16.1) |
| AD-011 | startup validator qua `RequiredPorts` (scope-aware, aggregate) | `RequiredPortsValidatorTests` | ✅ ENFORCED (task 10.2) |
| AD-012 | UoW reentrancy (nested join transaction) | `TransactionTests.*reentrant*` (nested join + nested rollback) | ✅ ENFORCED (task 6.2) |
| AD-013 | thứ tự middleware pipeline §3.5 | `BedrockPipelineTests` (correlation/mask → hàm ý thứ tự) | 🟡 PARTIAL (rate-limit/CORS slot → task 11) |
| AD-014 | `IEndpointModule` hợp đồng discovery | `BedrockPipelineTests` (discovery + map) | ✅ ENFORCED (task 5.4) |
| AD-015 | hợp đồng serialization Outbox (STJ cố định) | `OutboxWriterTests.Enqueue_serializes_payload_*` (camelCase, giữ field record dẫn xuất) | ✅ ENFORCED (task 7.2) |
| AD-016 | dispatcher atomic claim (skip-locked) + exponential backoff | `OutboxDispatcherTests.ComputeBackoff_*`/`Dispatch_*` (backoff/dead-letter) + `PostgresOutboxInboxTests.Concurrent_dispatchers_never_double_claim` (skip-locked đa-instance) | ✅ ENFORCED (task 7.3/7.4) |
| AD-017 | `IntegrationEvent` ở `Bedrock.Messaging.Contracts` (zero-dep) | `DecisionGuardTests.AD017_*` + `DependencyRuleTests` | ✅ ENFORCED |
| AD-018 | Scrutor/DI abstractions cho phép ở Application | `DependencyRuleTests` (Application không ref EF/ASP.NET) | 🟡 PARTIAL (kiểm mặt cấm; whitelist là chủ đích) |
| AD-019 | suppress `CA1000` cho `Result<T>` factory | build 0-warning gate (TreatWarningsAsErrors) | 🟡 IMPLICIT (build gate) |
| AD-020 | tách tên `NotFound`/`NotFoundGeneric` | build gate (CS0111 sẽ tái xuất nếu regress) | 🟡 IMPLICIT (build gate) |
| AD-021 | `IHtmlSanitizer` ở `Ports.Html` | `DecisionGuardTests.AD021_*` | ✅ ENFORCED |
| AD-022 | chia đôi enforcement CP1 (tên/namespace vs literal) | `NoBusinessInCoreTests` (tên/namespace) | ✅ RESOLVED (literal phần bù hoàn tất bởi AD-046/task 20) |
| AD-046 | CP1 literal scan qua Mono.Cecil (ldstr + const, 5 assembly Bedrock.*) | `NoBusinessInCoreLiteralTests` (+ negative control dirty-assembly) | ✅ ENFORCED (task 20) |
| AD-023 | claim JWT-native (`sub`/`role`/`permission`/`tenant_id`/`sid`) | `HttpContextCurrentUserTests` (behavioral) | ✅ ENFORCED |
| AD-024 | request-logging masked thay framework + Host hạ `Microsoft.AspNetCore`=Warning | `BedrockPipelineTests` (e2e no-leak WITH posture) | ✅ ENFORCED (e2e) + 🧠 MANUAL (posture Host — task 16.2) |
| AD-025 | dispatcher generic invoker (không MethodInfo.Invoke) | `DomainEventDispatchTests.Handler_throwing_*` (exception giữ nguyên kiểu) | ✅ ENFORCED (task 6.4) |
| AD-026 | `SaveChanges` đồng bộ bị chặn | `TransactionTests.SaveChanges_sync_is_blocked_*` | ✅ ENFORCED (task 6.1) |
| AD-027 | max dispatch depth → ném lỗi | `DomainEventDispatchTests.Exceeding_max_dispatch_depth_*` | ✅ ENFORCED (task 6.4) |
| AD-028 | pin transitive SQLitePCLRaw 3.50.3 (CVE) | build/restore gate (NuGetAudit + TreatWarningsAsErrors) | ✅ ENFORCED (build gate) |
| AD-029 | `OutboxMessage.Id = IntegrationEvent.Id` (idempotency đầu-cuối) | `OutboxWriterTests.Enqueue_serializes_payload_and_copies_event_fields` | ✅ ENFORCED (task 7.2) |
| AD-030 | cổng journal-consistency tự động (INV-1..5) | `JournalConsistencyTests` (tự gác chính nó) | ✅ ENFORCED (mỗi build) |
| AD-031 | `GetByHashAsync` trả cả token revoked (reuse-detection §7.4) | `RefreshTokenStoreTests.GetByHash_returns_revoked_token_for_reuse_detection` | ✅ ENFORCED (task 8.2) |
| AD-032 | ký JWT bằng `JsonWebTokenHandler` (không legacy handler — tránh static claim-map) | `JwtTokenServiceTests` (claim `sub`/`role` giữ nguyên tên + kid + rotation) | ✅ ENFORCED (task 9.2) |
| AD-033 | marker-scan DI tự viết bằng reflection (không Scrutor) | `RegistrationConventionTests` (lifetime đúng + loại IManualRegistration + self-fallback + duplicate-guard) | ✅ ENFORCED (task 10.1) |
| AD-034 | rate-limit 429 = edge concern (problem+json trực tiếp qua `OnRejected`) — REFINED bởi AD-078: RateLimited nay là first-class `ErrorType` (7 loại) → 429 có đường qua Result/ProblemDetails nhất quán, edge middleware vẫn dùng `CommonErrors.RateLimited()` cùng shape | `RateLimitIntegrationTests` (429 khi vượt) + `ErrorHandlingTests` (RateLimited→429) | ✅ ENFORCED (task 11.1; REFINED AD-078) |
| AD-035 | HSTS/HTTPS-redirect = trách nhiệm Host (base không ép) | (ràng buộc Host — kiểm ở task 16.2) | 🧠 MANUAL (Host posture) |
| AD-036 | field shape record External Auth (design chỉ đặt tên) | `DependencyRuleTests` (port zero-tech) + build 0-warning | 🟡 IMPLICIT (contract-first; behavior khi có adapter task 12.3-consumer) |
| AD-037 | Scrutor `Decorate` cho pipeline behaviors (§8); marker-scan giữ reflection (revisit AD-033/AD-018) | `PipelineOrderTests` (DI+Scrutor thật: thứ tự Logging→Authz→Validation→Idempotency→Transaction→UseCase; unauthorized+invalid→forbidden) | ✅ ENFORCED (task 15) |
| AD-038 | permission khai `[RequirePermission]` trên KIỂU input, ngữ nghĩa AND (least-privilege) | `AuthorizationDecoratorTests` (missing→forbidden, granted→run, no-attr→pass, AND-semantics, command variant, `PermissionMetadata.For`) | ✅ ENFORCED (task 15) |
| AD-039 | Idempotency v1: gate `IIdempotentCommand` + `TryBegin` (không replay) + TTL 24h → `idempotency_conflict` | `IdempotencyDecoratorTests` (first-run/duplicate-conflict/non-idempotent-skip-store/command-variant/ttl=24h) | ✅ ENFORCED (task 15) |
| AD-040 | Transaction behavior CHỈ `ICommandUseCase<>` (ghi thuần); value-returning tự quản (reentrancy AD-012) | `TransactionDecoratorTests` (body-inside-transaction, failing-propagates) + `PipelineOrderTests` (command mở transaction; query family không; unauthorized→0 transaction) | ✅ ENFORCED (task 15) |
| AD-041 | Refresh token lifetime mặc định 14 ngày (module Identity, promotable Options) | `RefreshAccessTokenUseCaseTests` (lifetime=14d + expiresAt=now+14d) | ✅ ENFORCED (task 16.1) |
| AD-042 | health-check DB đặt tên per-context `database:{TContext}` (multi-module không trùng) | `MultiModulePersistenceTests` (2 context → 2 tên `ready` duy nhất + HealthCheckService resolve không ném) | ✅ ENFORCED (task 16.2) |
| AD-043 | API versioning URL-segment `/v{version}` + `MapVersionedGroup` (default v1, report versions) | `HostSmokeTests` (`/v1/identity/token/refresh` → 400 + header `api-supported-versions: 1.0`) | ✅ ENFORCED (task 17) |
| AD-044 | correlation id = W3C traceId trace hiện hành (bỏ override client; propagation qua traceparent) | `BedrockPipelineTests.Unhandled_exception_*` (traceparent → header==body.traceId==traceId) | ✅ ENFORCED (task 18) |
| AD-045 | JWT validate-on-start (options `.ValidateOnStart()`, không eager); secret ngoài repo | `HostSmokeTests` (boot với secret inject; `Host_fails_fast_when_required_jwt_secret_is_missing` → boot fail "HS256") | ✅ ENFORCED (task 19) |
| AD-047 | Outbox retention: base cấp logic `PurgeAsync` (Host lên lịch); TTL mặc định processed 7d / dead-letter giữ vô thời hạn; vị từ xoá không đụng pending/dead-letter | `OutboxRetentionTests` (SQLite client-side branch) + `PostgresOutboxInboxTests.Retention_purge_*` (Testcontainers: nhánh Npgsql `ExecuteDeleteAsync` — xoá processed-cũ giữ pending/dead-letter; xoá dead-letter khi bật TTL) | ✅ ENFORCED (task 7.5/7.4) |
| AD-048 | Adapter RabbitMQ: topic exchange (routing=EventType) + publisher-confirms + persistent + resilience(timeout→retry→circuit) + Replace override + validate-on-start + channel reuse/serialize | `AdapterIsolationTests` (CP3) + `RabbitMqMessageMapperTests`/`RabbitMqResiliencePipelineTests`/`RabbitMqOptionsTests`/`AddRabbitMqMessagingTests` (13 test thuần) + `RabbitMqPublishIntegrationTests` (Testcontainers publish→consume) | ✅ ENFORCED (task 14) |
| AD-049 | claim Outbox Npgsql bằng raw SQL `FOR UPDATE SKIP LOCKED` (hiện thực CP15; tên bảng từ model) | `PostgresOutboxInboxTests.Concurrent_dispatchers_never_double_claim` (2 dispatcher đồng thời → mỗi message publish đúng 1 lần) | ✅ ENFORCED (task 7.4) |
| AD-050 | vận hành hoá: EF migrations per-module (design-time factory) thay EnsureCreated; migrate out-of-band | `Identity.IntegrationTests/IdentityMigrationTests` (Testcontainers: MigrateAsync → schema identity + __EFMigrationsHistory + round-trip store) | ✅ ENFORCED (post-base) |
| AD-051 | Dockerfile Host multi-stage/non-root; secret qua env (không bake); không auto-migrate | docker build + `docker run` (fail-fast khi thiếu secret; `/health/live`=200 khi có secret) verified in-session 2026-07-10 + CI job `docker-image` | 🧠 MANUAL/CI (không phải dotnet test — hạ tầng đóng gói) |
| AD-052 | CI (GitHub Actions): tool-restore + build 0-warning + full test (Testcontainers) + docker build | `.github/workflows/ci.yml` (inspection-verified; ubuntu-latest có Docker → integration chạy thật; gồm JournalConsistencyTests) | 🧠 MANUAL/CI (chạy trên runner, không cục bộ) |
| AD-053 | opt-in `Bedrock:ApplyMigrationsOnStartup` (mặc định TẮT) cho dev/compose; prod out-of-band | `HostSmokeTests` (cờ tắt → boot không migrate, xanh) + compose verified (cờ bật → /health/ready=200) | ✅ ENFORCED (default-off) + 🧠 MANUAL (nhánh bật: compose) |
| AD-054 | docker-compose full-stack (Postgres + Host) chứng minh chạy end-to-end | `docker compose up` → postgres healthy → host migrate → /health/ready=200 verified in-session 2026-07-10 | 🧠 MANUAL/CI (tích hợp cả hệ — không phải dotnet test) |
| AD-055 | EF migration bundle self-contained = artifact migrate out-of-band prod (đóng loop AD-050) | bundle áp `InitialCreate` → 3 bảng schema identity verified in-session 2026-07-10 + CI job `migration-bundle` (upload artifact linux-x64) | 🧠 MANUAL/CI (artifact deploy — không phải dotnet test) |
| AD-056 | worker LÊN LỊCH dispatcher outbox = opt-in ở base (`AddOutboxDispatcherWorker<TContext>`), Host bật qua `Bedrock:Messaging:Enabled` (mặc định TẮT); REFINES AD-047 (Host quyết lịch, base KHÔNG tự chạy) | `Messaging.IntegrationTests/OutboxDispatcherWorkerEndToEndTests` (Testcontainers Postgres+RabbitMQ: seed outbox → worker qua đường thật `AddOutboxDispatcherWorker`→`AddHostedService`→`BackgroundService` TỰ ĐỘNG phát+mark, KHÔNG dispatch thủ công) + `StarHill.Api.Tests` (messaging TẮT → boot xanh, không hồi quy) | ✅ ENFORCED (post-base) |
| AD-057 | EMIT `UserTokenRefreshedIntegrationEvent` từ use case rotation qua `IOutboxWriter` trong cùng transaction (chỉ khi thành công); đóng N-040 | `Identity.UnitTests/RefreshAccessTokenUseCaseTests` (emit-on-success đúng field + enqueue trước SaveChanges + `Failed_rotations_do_not_emit_event` 4 nhánh fail không emit) + `Identity.IntegrationTests/RefreshRotationEmitsEventTests` (Postgres THẬT: rotation → row outbox `identity.user_token_refreshed`, payload có UserId không có token thô, persist cùng transaction đọc ở scope mới) | ✅ ENFORCED (post-base) |
| AD-058 | docker-compose capstone: RabbitMQ + messaging opt-in → chuỗi Outbox→worker→RabbitMQ chạy trên IMAGE BUILD THẬT; dùng user `bedrock` (không `guest` — loopback-refuse) | `docker compose up --build` verified in-session 2026-07-10 (healthy + worker-started log + seed outbox → `processed_at` non-null, `error_count=0` = publish OK tới broker compose) | 🧠 MANUAL/CI (artifact+compose — không phải dotnet test; code path đã phủ bởi Testcontainers AD-056/057/048) |
| AD-059 | nửa CONSUME: port `IIntegrationEventDispatcher` (Application) + core agnostic `EfIntegrationEventDispatcher` (Infrastructure) — registry resolve→inbox+handler cùng transaction→ack/dup/dead-letter; §7.3/F30 | `Bedrock.Infrastructure.Tests/PostgresIntegrationEventDispatcherTests` (Postgres: handled-once+inbox / redeliver→Duplicate handler 1 lần / unknown→DeadLettered không inbox) + `AdapterIsolationTests` (CP3 giữ vì logic ở port Application) + `UseCaseSeamTests`/`ModuleBoundaryTests` (CP11: port ở Messaging.Dispatch) | ✅ ENFORCED (post-base) |
| AD-060 | subscriber RabbitMQ (adapter BackgroundService) + topology tối thiểu Host + NACK requeue=false (DLX cho prod) | `Messaging.IntegrationTests/RabbitMqConsumeEndToEndTests` (Postgres+RabbitMQ: publish→subscriber thật→dispatch→handler 1 lần+inbox persist) + `AdapterIsolationTests` (CP3: adapter chỉ ref Application, Hosting.Abstractions không bị cấm) | ✅ ENFORCED (post-base) |
| AD-061 | CI hardening: gate `develop` + concurrency cancel-in-progress + least-privilege permissions + job timeouts (REFINES AD-052 — cổng anti-drift chạy trên nhánh tích hợp thật) | `.github/workflows/ci.yml` (PyYAML parse OK + assert push.branches⊇develop/concurrency/permissions/timeouts in-session; git-evidence 0-merge chứng minh gap) | 🧠 MANUAL/CI (chạy trên runner khi push develop — không cục bộ; verify cú pháp+schema+git-evidence) |
| AD-062 | Command-governance: launcher cố định `scripts/vp.cmd`→`tools/verify.ps1`+`tests/validate_ci.py` (không one-liner ad-hoc; logic mới vào script cố định) | `tests/validate_ci.py` (chạy trong `vp ci` — validate bất biến ci.yml AD-061) + `scripts\vp.cmd` verified in-session (build/ci/test đều OK, exit 0) | 🧠 MANUAL/CI (lệnh dev `vp`; anti-drift tầng thao tác) |
| AD-063 | Fix gốc: Testcontainers fixture build container LAZY trong try → thiếu Docker thì SKIP (hợp đồng N-012), không FAIL | `scripts\vp.cmd` (full suite trên máy KHÔNG Docker → 235 total, **0 failed, 16 skipped**; trước fix 10 failed) + `[SkippableFact]`+`Skip.IfNot(Available)` mỗi test integration | ✅ ENFORCED (verify FAIL→SKIP in-session; no-op khi có Docker) |
| AD-064 | RabbitMQ readiness health-check (§9.6/R34) tag "ready" + gom `RabbitMqConnectionFactory` dùng chung | `AddRabbitMqMessagingTests.Registers_rabbitmq_readiness_health_check_tagged_ready` (không Docker) + `RabbitMqPublishIntegrationTests.Health_check_reports_healthy_when_broker_reachable` (SkippableFact, broker thật) + `AdapterIsolationTests` (CP3 giữ) | ✅ ENFORCED (đăng ký verify cục bộ; hành vi broker → CI/Docker) |
| AD-065 | Move `BedrockTelemetry`→Application (shared) + metric Outbox (published/dead_lettered/publish-lag) dưới Meter "Bedrock" (R24.3/§7.2; đóng N-046) | `Bedrock.Infrastructure.Tests/OutboxMetricsTests` (MeterListener trên SQLite, collection DisableParallelization — published+lag khi success, dead_lettered khi max-attempts) + telemetry test cũ vẫn xanh | ✅ ENFORCED (verify cục bộ không-Docker) |
| AD-066 | Metric CONSUME (`bedrock.inbox.dispatched` tag outcome + `processing.duration`) trong dispatch core (R24.3 consumer time; đối xứng AD-065) | `Bedrock.Infrastructure.Tests/InboxMetricsTests` (SQLite, 4 outcome handled/duplicate/dead_lettered/failed; collection MessagingMetricsSerial DisableParallelization) | ✅ ENFORCED (verify cục bộ không-Docker) |
| AD-067 | Bộ security headers base (nosniff/DENY/no-referrer/none), CSP để app; §3.5 slot #5 | `Bedrock.Api.Tests/HttpSecurity/SecurityHeadersTests` (Theory /ok 200 + /boom 500 → 4 header+giá trị có mặt cả trên response lỗi) | ✅ ENFORCED (e2e không-Docker; đóng lỗ hổng keystone) |
| AD-068 | OpenAPI doc-gen native (.NET 10) OPT-IN — `/openapi/v1.json`; hoàn tất phần base DV-015; pin CVE Microsoft.OpenApi 2.7.5 | `Bedrock.Api.Tests/OpenApi/BedrockOpenApiTests` (opt-in→200+document endpoint versioned "oa/ping"; không opt-in→404) | ✅ ENFORCED (e2e không-Docker) |
| AD-069 | dịch vi phạm UNIQUE (Npgsql 23505) → `UniqueConstraintViolationException` trung lập (song song concurrency); Api map 409 | `RefreshTokenRotationRaceTests.Consume_and_insert_roll_back_together_when_insert_conflicts` (Testcontainers/Postgres: insert trùng hash qua UoW → `UniqueConstraintViolationException` + rollback) | ✅ ENFORCED (Testcontainers/CI; SQLite giữ DbUpdateException — filter Postgres-only) |
| AD-070 | `.editorconfig generated_code=true` cho `[**/Persistence/Migrations/*.cs]` (miễn analyzer style/quality cho code EF sinh; fix gốc CA1861 migration mới) | build 0-warning gate (`TreatWarningsAsErrors`) — migration `AddOutboxClaimLease` composite index `new[]{...}` không còn phá build | 🟡 IMPLICIT (build gate; mirror QR-AD-009) |
| AD-071 | `JwtKeyRingOptions` đăng ký IDEMPOTENT+LAZY (guard theo concrete) → sign (Infra) + verify (Api) chia sẻ 1 instance đã validate; fix regression eager-empty shadow (A-18) | `StarHill.Api.Tests/HostSmokeTests` (boot với secret inject → 200 + `Host_fails_fast_when_required_jwt_secret_is_missing`) + `Bedrock.Api.Tests/AuthMechanismTests` (verify JWT standalone không cần AddBedrockSecurity) | ✅ ENFORCED (REFINES AD-008/AD-045) |
| AD-072 | Outbox dispatch dùng LEASE: claim ngắn (ClaimId/ClaimedUntil) → publish ngoài transaction → finalize guard-by-lease → lease-expiry recovery (A-08 — không giữ DB lock khi gọi broker) | `OutboxDispatcherTests.{Expired_lease_is_reclaimed_and_published, Active_lease_held_by_other_instance_is_not_claimed}` (SQLite local) + `PostgresOutboxInboxTests.Concurrent_dispatchers_never_double_claim` (CP15, Testcontainers) | ✅ ENFORCED (REFINES AD-016/AD-049) |
| AD-073 | RabbitMQ reliability (A-03/04/05): publisher gate-wraps-publish+reconnect+mandatory-confirm (unroutable KHÔNG mark processed); consumer broker-side DLX/DLQ, NACK requeue=false quarantine malformed/unknown/handler-fail (không drop) | `RabbitMqConsumerOptionsTests` (fail-fast + DLQ naming, local) + `RabbitMqPublishIntegrationTests`/`RabbitMqConsumeEndToEndTests` (runtime broker, Testcontainers/CI) | ✅ ENFORCED local (options) + 🧠 V2/Docker (runtime); REFINES AD-048/AD-060 |
| AD-074 | Idempotency Complete/Abort + key namespace (operation-type+principal); Failure/exception → Abort nhả key (hết "claim rồi quên") | `IdempotencyDecoratorTests.{Failed_inner_releases_key_so_retry_is_allowed, Different_input_types_with_same_raw_key_do_not_collide}` (+ first-run/duplicate-conflict/command-variant) | ✅ ENFORCED (REFINES AD-039) |
| AD-075 | Domain-event dispatch LUÔN trong explicit transaction (EfUnitOfWork wrap khi chưa có) + snapshot/restore event khi handler ném (không mất event) | `DomainEventDispatchTests.Domain_events_are_restored_after_handler_failure` (+ CP14 handler-effect-atomic/handler-throw-rollback/max-depth/no-handler) | ✅ ENFORCED (REFINES AD-007/AD-012/CP14) |
| AD-076 | Fail-fast validate-on-start MỌI options family (HttpSecurity/PasswordHashing/Outbox Dispatcher-Worker-Retention/Observability + JWT/RabbitMq) — A-16 | `OptionsValidationTests` (PasswordHashing + Outbox×3) + `HttpSecurityOptionsTests` (IP/CIDR/rate-limit/CORS) + `RabbitMqConsumerOptionsTests`/`RabbitMqOptionsTests` | ✅ ENFORCED (R13.3/R25.2; bổ trợ AD-045/AD-034/AD-048) |
| AD-077 | Argon2 PHC: IsWithinBounds TRƯỚC Compute (reject-before-alloc, chống DoS) + enforce v=19 + password ≤4096B — A-17 | `PasswordHasherTests.{Verify_rejects_tampered_hash_with_out_of_bounds_params, Verify_rejects_unsupported_version, Oversized_password_is_bounded}` | ✅ ENFORCED (bổ trợ AD-076; §5.7) |
| AD-078 | Error invariant code/message non-empty (ctor) + RateLimited thành first-class ErrorType (→429) + CommonErrors.RateLimited — A-26 | `ErrorTests` (blank→throw, None-empty-OK, factory→type) + `ErrorHandlingTests.ToStatusCode_should_map_each_error_type` (RateLimited→429) | ✅ ENFORCED (REFINES AD-034; CP12) |
| AD-079 | ExceptionHandlingMiddleware: client-abort (OCE+RequestAborted) nuốt êm (không log/không 500); OCE-khác→500; Response.HasStarted→rethrow gốc — A-27 | `ExceptionHandlingMiddlewareTests.{Client_aborted_cancellation_is_swallowed_not_500, Cancellation_not_caused_by_abort_becomes_500, Unhandled_exception_becomes_500_problem_details}` | ✅ ENFORCED (§3.5 slot #3) |
| AD-080 | soft-delete = NAMED query filter (`SoftDeleteFilterName`) → compose AND với filter module thay vì ghi đè; audit Created*/CreatedByUserId `IsModified=false` ở Modified → bất biến trước caller — A-25 | `ConventionTests.Audit_created_metadata_is_protected_from_caller_tampering` + `SoftDeleteFilterCompositionTests.Module_named_filter_composes_with_soft_delete_filter_instead_of_overriding` | ✅ ENFORCED (REFINES persistence conventions §7.5) |
| AD-081 | port `IEventBusPublisher` nhận envelope BẤT BIẾN `OutgoingIntegrationMessage` (id/type/version/payload/occurred/correlation), KHÔNG lộ cột retry/persistence của `OutboxMessage` cho adapter bus — A-20 | `DecisionGuardTests.AD081_EventBusPublisher_port_takes_immutable_outgoing_envelope_not_outbox_record` | ✅ ENFORCED (design §5.2/§7.2; F29) |
| AD-082 | consume-side: `IncomingIntegrationMessage` mang `ContentType`/`SchemaVersion`; dispatcher agnostic validate content-type == JSON TRƯỚC handler → lạ/thiếu = DeadLettered (quarantine); id-match integrity khoá — A-10 | `IntegrationEventEnvelopeValidationTests` (non-json/missing→DeadLettered, json+charset→Handled, id-mismatch→throw) | ✅ ENFORCED (design §5.2/§7.3; version-compat=A-21, trace=A-29) |
| AD-083 | registry integration-event: enforce hợp đồng metadata field-independent (EventType đọc-được/non-empty + SchemaVersion≥1 + unique) qua build-gate; GIỮ key=EventType (KHÔNG (EventType,SchemaVersion)) vì versioning R22.3 (breaking→EventType mới + tolerant reader) — A-21 | `IntegrationEventMetadataContractTests` (field-independent metadata + unique) + hardening `IntegrationEventTypeRegistry.ReadEventType` | ✅ ENFORCED (R22.2/R22.3, §9.1; AD-006) |
| AD-084 | trace xuyên bus: dispatcher agnostic tạo consumer span (`ActivityKind.Consumer`) là CON của trace gốc parse từ `CorrelationId` (W3C traceparent); `IncomingIntegrationMessage` mang CorrelationId — A-29 | `IntegrationEventTracePropagationTests` (ActivityListener: consume span cùng TraceId + ParentSpanId = trace gốc; graceful khi thiếu CorrelationId) | ✅ ENFORCED (F34/F21, §7.3; gauge/external-auth defer A-28) |
| AD-085 | `AddBedrockCore` idempotency-guarded: gọi lần 2 → NÉM (chống double-decoration pipeline + validator trùng); sentinel `BedrockCoreMarker` — A-12 | `BedrockCoreIdempotencyTests` (gọi-1-lần OK; gọi-2-lần throw fail-loud) | ✅ ENFORCED (§8/AD-037; F35/R13) |
| AD-086 | hợp đồng giao Outbox = at-least-once, KHÔNG cam kết ordering (occurred_at chỉ best-effort claim); poison độc-lập KHÔNG chặn message khác — A-09 | `OutboxDispatcherTests.Poison_message_does_not_block_other_messages_in_batch` (độc-lập per-message) + docstring `IOutboxDispatcher` (unordered là doc/MANUAL) | ✅ ENFORCED (độc-lập); 🧠 MANUAL (phát biểu unordered — design §7.2) |
| AD-087 | outbox `last_error`/`last_attempt_at` (sanitize kiểu+message, bound 1024) ghi ở mọi lần publish fail (backoff+dead-letter) — A-28 | `OutboxDispatcherTests.{Dispatch_failure_records_last_error_and_last_attempt, Dead_letter_records_last_error}` + migration `AddOutboxLastError` | ✅ ENFORCED (AD-050 migration; R8.5) |
| AD-088 | auth-model = JWT bearer token-in-body (base không auth-cookie → CSRF-cookie N/A); cookie/CSRF/CORS coherent + opt-in (Validate cấm wildcard+credentials; antiforgery X-CSRF-TOKEN; CORS siết theo mode) — A-15 | `HttpSecurityOptionsTests` (Validate) + `HttpSecurityConfigTests` (cookie flags + antiforgery-by-mode) | ✅ ENFORCED (options+registration); 🧠 MANUAL (CORS behavioral e2e chưa phủ) |
| AD-089 | contract snapshot descriptor CANONICAL: generic args đầy đủ + array + nullability value/reference (NullabilityInfoContext) → bắt breaking-change tinh vi — A-22 | `ContractSchemaPrecisionTests` (generic-arg/array/value-nullable/reference-nullable) + `IntegrationEventSchemaSnapshotTests` dùng `ContractSchema` | ✅ ENFORCED (F32/R32.4; auto-discover=A-11) |
| AD-090 | gỡ build-artifact (bin/obj, 4238 file) khỏi Git index (`git rm --cached`, giữ file đĩa) + guard chống tái-track — A-30 | `validate_ci.py::validate_no_tracked_artifacts` (vp ci: tracked bin/obj>0 → FAIL) | ✅ ENFORCED (vp ci build-gate; AD-062) |
| AD-091 | [P0 SECURITY] Forwarded Headers chỉ xử lý X-Forwarded-* khi có proxy/network tin cậy; không có → `None` (chống spoof IP, an-toàn-mặc-định) — re-audit P0-01 | `ForwardedHeadersTrustTests` (untrusted-peer không spoof; trusted-proxy honor) + `RateLimitIntegrationTests` | ✅ ENFORCED (behavioral TestServer; F16) |
| AD-092 | [Gate 0] idempotency scope key gồm CẢ tenant VÀ user (không fallback) + hash SHA-256 rawKey (bound + không rò) — re-audit P1-03 | `IdempotencyDecoratorTests.{Same_key_same_tenant_different_users_do_not_collide, Same_key_same_identity_still_conflicts}` | ✅ ENFORCED (REFINES AD-074; fencing/replay defer Wave 1) |
| AD-093 | [Gate 0] schema-version invariant: thiếu/lỗi/overflow → 0; dispatcher quarantine nếu `<1` (không default-1/overflow-wrap) — re-audit P1-02 | `IntegrationEventEnvelopeValidationTests.Invalid_schema_version_is_dead_lettered` (0, -1) | ✅ ENFORCED (REFINES AD-082/AD-083; R22.3) |
| AD-094 | [Gate 0] domain-event failure boundary bao TRỌN dispatch→convention→base-save; ném bất kỳ đâu sau dequeue → restore mọi event (không mất) — re-audit P1-01 | `DomainEventDispatchTests.Domain_events_restored_when_convention_or_save_fails_after_dispatch` | ✅ ENFORCED (mở rộng AD-075; CP14/R33) |
| AD-095 | [Gate 0] consumer retry tier: transient→retry có delay + giới hạn (`x-bedrock-attempt`, retry-queue TTL→main), permanent (DeadLettered/JsonException)→DLQ ngay; retry-publish channel confirms (không mất khi hiccup) — re-audit P0-02 | `RabbitMqDeliveryPolicyTests` (exhaustive ForOutcome/ForException/IsPermanent + ngưỡng attempt) + `RabbitMqConsumerOptionsTests` (MaxDeliveryAttempts/RetryDelay/retry-exchange) + e2e `RabbitMqConsumeEndToEndTests.Transient_handler_failure_is_retried_with_delay_then_succeeds` (Testcontainers/CI) | ✅ ENFORCED (liên quan AD-073/082/093; CP11/F29) |
| AD-096 | [Wave 1] Error invariant fields (Code/Message/Type) GET-ONLY → không bypass constructor-validation qua record `with` — re-audit P1-08 | `ErrorTests.{Invariant_fields_have_no_public_setter, Details_remains_settable_for_with_expression, WithDetails_preserves_core_fields_and_attaches_details}` | ✅ ENFORCED (refine AD-078/A-26; F20/R29) |
| AD-097 | [Wave 1] Outbox last_error REDACT thật (URI-creds/kv nhạy cảm/Bearer) + classification + bound, không chỉ truncate — re-audit P1-07 | `OutboxErrorFormatterTests` (13: creds/kv/bearer/bound/null/no-op) | ✅ ENFORCED (refine AD-087/A-28; F25/F33) |
| AD-098 | [Wave 1] `ICommandUseCase<TInput>` void kế thừa `ITransactionalUseCase` → COMPILE-ENFORCE PersistenceKey (đóng gap review P1; command void keyed không còn resolve nhầm UoW unkeyed lúc runtime) | COMPILE-GATE (build fail nếu command void thiếu `PersistenceKey`) + `KeyedCommandPipelineTests` (full-DI: keyed void→UoW đúng module; null→unkeyed legacy) | ✅ ENFORCED (compile + full-DI; §8 Transaction) |
| AD-099 | [Wave 1/A-11 partial] Discovery guard: adapter cấm PackageReference/FrameworkReference tech (EF/Npgsql/ASP.NET) + mọi module/adapter phải có trong Platform.slnx | `DiscoveredProjectBoundaryTests.{Every_discovered_adapter_forbids_technology_package_and_framework_references, Every_discovered_module_and_adapter_project_is_registered_in_solution}` | ✅ ENFORCED (project-graph; bổ trợ CP3; A-11 PARTIAL) |

## Cổng journal-consistency (INV-1..INV-5) — L4 tự động (AD-030)

`Bedrock.ArchitectureTests/JournalConsistencyTests` đọc chính các file journal (`01`–`05`) và biến kỷ luật tài liệu thành **build gate** (trước đây thủ công → nay `dotnet test` bắt). Nếu không tìm thấy thư mục journal (đi lên cây từ thư mục test không gặp `.kiro/specs/platform-base/journal`), test **`Assert.Fail`** rõ ràng — KHÔNG skip: cổng không được tự tắt âm thầm (skip = vô hiệu ngầm = drift ẩn). Test là dev-time nên luôn chạy trong repo có `.kiro`.

| INV | Bất biến | Vì sao (root cause của loại drift bị chặn) |
|---|---|---|
| INV-1 | Mỗi loại ID (AD/DV/TO/N) **duy nhất + liên tục `1..N`** | phát hiện bản ghi bị mất, nhân đôi, hoặc đánh số lại sai |
| INV-2 | Mọi `AD-###` (01) **có mặt trong bảng guard 05** | KEYSTONE RULE tự động: không thể thêm quyết định mà quên khai guard |
| INV-3 | Mọi ref `AD/DV/TO/N-###` trong journal **trỏ tới bản ghi có thật** | chống ID dangling / gõ nhầm một mã không tồn tại |
| INV-4 | Mỗi bản ghi **AD & DV** có đủ `Status:` + `Provenance/Evidence:` | chống bịa — mọi quyết định/độ lệch phải có bằng chứng nguồn |
| INV-5 | Mọi `CP##` tham chiếu trong journal nằm trong **1..15** | chống CP dangling (CP1–CP15 là tập đóng, N-011) |

> **Chốt logic:** L4 xưa là khâu yếu nhất (dựa trí nhớ). INV-1..5 khép nó vào cùng cổng `dotnet test` với code — đúng tinh thần "con người quên, build thì không", nay áp cho CẢ tài liệu.

## Khoảng hở chưa auto-enforce (nói thật, không giấu)

- ~~**CP1 string-literal**~~: ĐÃ ĐÓNG (task 20/AD-046) — `NoBusinessInCoreLiteralTests` quét ldstr + const bằng Mono.Cecil trên cả 5 assembly `Bedrock.*` (có negative control). CP1 nay phủ cả tên/namespace lẫn literal.
- **CP15 exclusive-claim + CP6 full + CP8 race**: dispatcher backoff/threshold/dead-letter/lọc-tới-hạn + inbox idempotency đơn-luồng ĐÃ enforced (task 7.3, SQLite); nhưng **claim skip-locked đa-instance + race Postgres thật** chờ task 7.4 (Testcontainers).
- **CP7 + publish integration**: CP3 (adapter isolation) NAY ĐÃ enforced (`AdapterIsolationTests`, task 14). Còn lại phần cần Docker/Testcontainers: RabbitMQ publish→consume thật (task 14 còn lại), rotation race đa-connection CP7 (task 8.3), Outbox race/claim CP6/CP8/CP15 (task 7.4). CP4/CP5/CP9/CP12 đã enforced. Mỗi task còn lại có dòng "Nghiệm thu" + `_Correctness Properties_` buộc thêm guard khi làm.
- **INV-4b (getDiagnostics format)**: kiểm heading/EARS/waves của Kiro KHÔNG chạy được trong `dotnet test` (là diagnostic của Kiro IDE) → vẫn thủ công sau mỗi sửa spec (L4b).
- **Bản sao lồng `StarHillGuestApp/StarHillGuestApp/`** (N-030): một cây `.kiro` cũ song song, stale → rủi ro sửa nhầm bản. `JournalConsistencyTests` chỉ gác bản GỐC (nơi có `platform/`). Cần người xác nhận/gỡ (không tự xoá).
- **AD tài liệu thuần** (vd quyết định lộ trình): không code-enforceable → INV-2 vẫn buộc có mặt trong bảng guard với trạng thái `IMPLICIT`/`MANUAL` (không được biến mất khỏi radar).

## Thêm một guard mới (thủ tục)

1. Xác định quyết định/CP có code-enforceable không. Nếu có → viết test ở đúng project:
   - Cấu trúc/dependency/naming → `Bedrock.ArchitectureTests` (`DecisionGuardTests` hoặc file luật) hoặc `Bedrock.Api.Tests/Architecture` (cho luật chạm Api).
   - Hành vi → test project tầng tương ứng.
2. Mỗi luật NetArchTest phải có **negative control** (chứng minh bắt được vi phạm).
3. Cập nhật 2 bảng guard ở trên (CP hoặc AD) + trạng thái.
4. Chạy vòng lặp bắt buộc.

> **Tóm tắt một câu:** *Drift bị chặn mạnh nhất khi mỗi quyết định là một test — con người quên, build thì không.*
